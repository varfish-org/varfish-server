from django.apps import AppConfig

# DEPRECATED WARNING!
# The HGMD app is discontinued and regarded as legacy.
# We keep the app to keep the migrations.


class HgmdConfig(AppConfig):
    name = "hgmd"
