We keep the OpenAPI schema in the repository for additional sanity checks and tracking changes in git.

The file is generated by:

```
python manage.py generateschema \
> varfish/tests/drf_openapi_schema/varfish_api_schema.yaml
```
